import React from "react";
import Typography from "antd/es/typography/Typography";

const { Title } = Typography;

const Exchanges = () => {
  return <Title level={2}>This API route is now tolled :(</Title>;
};

export default Exchanges;
